$(document).ready(function(){
  $('.galeria-notex-1').slick({
  dots: true,
  autoplaySpeed: 5000,
  infinite: true,
  autoplay: true,
  slidesToShow: 1,
	});

  $('.galeria-notex-2').slick({
  autoplay: true,
  autoplaySpeed: 5000,
  dots: true,
  infinite: true,
  slidesToShow: 1,
	});

  $('.galeria-notex-3').slick({
  autoplay: true,
  autoplaySpeed: 5000,
  dots: true,
  infinite: true,
  slidesToShow: 1,
	});

  $('.galeria-home').slick({
  autoplay: true,
  autoplaySpeed: 5000,
  dots: true,
  infinite: true,
  slidesToShow: 1,
	});

  // $('.hero').slick({
  // autoplay: true,
  // autoplaySpeed: 3000,
  // dots: false,
  // infinite: true,
  // slidesToShow: 1,
  // accessibility: true,
  // });

});
